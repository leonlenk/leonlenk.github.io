# custom
src/content

# Node.js
node_modules/
dist/
.astro/
.cache/
out/
build/
.tsbuildinfo
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-store/ # This is pnpm's global content-addressable store. If you're in a monorepo, it might be at the root.

# pnpm-specific
pnpm-lock.yaml # You generally want to commit this, but if for some reason you don't, add it here.
.pnpm/ # Local symlink store for the project
.npmrc # If you have sensitive info or local overrides that shouldn't be committed

# Astro-specific
.astro/
.output/ # If you're building for serverless or static output

# TypeScript
*.tsbuildinfo
*.js.map
*.d.ts
!src/**/*.d.ts # Keep declaration files within src if you're generating them there for distribution
!src/**/*.js   # Keep JS files within src if you're intentionally committing them

# Editor/OS specific
.DS_Store
.env
.env.local
.env.*.local
.vscode/ # VSCode settings, extensions, etc. You might want to commit .vscode/settings.json or .vscode/extensions.json
.idea/ # IntelliJ IDEA
.history/ # VS Code Local History
*.sublime-project
*.sublime-workspace
Thumbs.db

# Other common ignores
.DS_Store
coverage/
cypress/videos/
cypress/screenshots/
test-results/

# You might want to exclude these if generated by tools
.svelte-kit/ # If you're integrating Svelte with Astro and using SvelteKit
.next/ # If you're integrating Next.js with Astro for some reason